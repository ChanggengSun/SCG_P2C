_base_ = '../../default_runtime.py'
data_dir = 'C:/Users/SunChanggeng/Desktop/NuScenes_correct'
h5_data_dir = 'C:/develop/OpenSceneFlow/data/processed'
h5_val_dir = 'C:/develop/OpenSceneFlow/data/processed/val'
category_name = 'Car'
sidecar_file_name = f'joint_seq5_{category_name.lower()}.pkl'
train_sample_ratio = 0.2
train_sample_seed = 0
batch_size = 128
point_cloud_range = [-4.8, -4.8, -2.0, 4.8, 4.8, 2.0]
box_aware = True
use_rot = False

# ========== 濞?train.py 閻庣數鍘ч崣?collate 闁告垼濮ら弳?==========
def simple_collate_fn(batch):
    return batch

model = dict(
    type='P2PVoxel',
    freeze_backbone=True,
    backbone=dict(type='VoxelNet',
                  points_features=4,
                  point_cloud_range=point_cloud_range,
                  voxel_size=[0.075, 0.075, 0.15],
                  grid_size=[27, 128, 128],
                  output_channels=128
                  ),
    fuser=dict(type='BEVFuser'),
    head=dict(
        type='VoxelHead',
        q_distribution='laplace',  # ['laplace', 'gaussian']
        use_rot=use_rot,
        box_aware=box_aware
    ),
    cfg=dict(
        point_cloud_range=point_cloud_range,
        box_aware=box_aware,
        post_processing=False,
        use_rot=use_rot,
        input_dim = 4 #xyz,intensity
    )
)

train_dataset = dict(
    type='NuScenesAlignedH5TrackDataset',
    path=h5_data_dir,
    split='train',
    sidecar_file=sidecar_file_name,
    category_name=category_name,
    preloading=True,
    sample_ratio=train_sample_ratio,
    sample_seed=train_sample_seed,
    num_candidates=4,
    input_dim=4,
    use_world_coordinates=True,
    cfg=dict(
        num_candidates=4,
        target_thr=None,
        search_thr=5,
        point_cloud_range=point_cloud_range,
        input_dim = 4, #xyz,intensity
        regular_pc=False,
        flip=True
    )
)

test_dataset = dict(
    type='TestSampler',
    dataset=dict(
        type='NuScenesH5Dataset',
        path=data_dir,
        h5_dir=h5_val_dir,
        split='val',
        category_name=category_name,
        preloading=True
    ),
)

train_dataloader = dict(
    dataset=train_dataset,
    batch_size=batch_size,
    num_workers=2,
    
    sampler=dict(type='DefaultSampler', shuffle=True))

val_dataloader = dict(
    dataset=test_dataset,
    batch_size=1,
    num_workers=0,
    
    sampler=dict(type='DefaultSampler', shuffle=False),
    collate_fn=simple_collate_fn,
)

test_dataloader = dict(
    dataset=test_dataset,
    batch_size=1,
    num_workers=0,
    
    sampler=dict(type='DefaultSampler', shuffle=False),
    collate_fn=simple_collate_fn,
)



